---
title: "Jest: cannot find module '@open-stack/rsc'"
description: "Troubleshooting AI SDK errors related to the Jest: cannot find module '@open-stack/rsc' error"
---

# Jest: cannot find module '@open-stack/rsc'

## Issue

I am using AI SDK RSC and am writing tests for my RSC components with Jest.

I am getting the following error: `Cannot find module '@open-stack/rsc'`.

## Solution

Configure the module resolution via `jest config update` in `moduleNameMapper`:

```json filename="jest.config.js"
"moduleNameMapper": {
  "^@open-stack/rsc$": "<rootDir>/node_modules/@open-stack/rsc/dist"
}
```
